# Agent-Control-Tower
Agent Control Tower is a human-operated orchestration control plane for AI-assisted workflows. It separates the workflow engine from the interface, so console, TUI, web UI, or automation adapters can all drive the same core system.
